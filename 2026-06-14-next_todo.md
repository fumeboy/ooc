我刚才校准了 context 设计（.ooc-world-meta/stones/main/objects/supervisor/children/thinkable/knowledge/context.md）

在完成后，预期做这些工作:

1. persistable ，目前是在 @ooc/core 下实现的，我考虑，也允许 ooc object 自定义如何持久化，当前的 @ooc/core 下的实现变成 ooc object 的 可覆盖的、默认的 持久化方式。而 builtin class/object 可以通过这种自定义接口，来决定自己如何持久化表示，提出这个设计，是考虑到，我们已经将 thread 也当作了一种 ooc object 在看待了，那么 thread 有自己的一套持久化方式，预期为 thread 特殊化，不如把这种特殊化变成 ooc object 的个性化自定义能力

2. 将系统内置的 context window 重新梳理一遍，整理到 packages/@ooc/builtins, 以 ooc class/object 的模式，core 开放接口供这些 builtin object 的 method 调用

3. 原先 @ooc/core 下有些语义化的目录，比如 reflectable, 这个目录下其实就是两个 builtin class/object, 这两个目录 pr 和 reflect-request 应该移动到 @ooc/builtins 目录下，但是原来的 reflectable 目录可以保留，index.ts 持有import 引用，以一种源代码index的形式存在，以免我们看代码的时候，想找 reflectable 这个概念相关的代码不知道在哪找

4. 目前 ooc object 的 constructor 设计还不够清楚，应该明确出 ooc class 与 ooc object, ooc class 也要明确出 单例 与 非单例，如果是 单例，那么 ooc class 就是 ooc object, 如果不是单例，那么 ooc class 需要提供 constructor 函数

5. 之前说，要将 thinkable 、collaborable、reflectable 能力维度从 ooc object 抽离，放在 ooc agent, ooc agent 继承自 ooc object, ooc agent 才具有和 LLM 交互（以及相关的智能能力）的能力，然后像 supervisor 这样的 agent 又继承自 ooc agent。 但是我看 packages/@ooc/builtins/root （ooc object 基类）并没有改彻底，还需要调整

一切的目标，都是为了让 OOC 系统，更好地实现表达出 从 OOP 继承而来的面向对象的哲学与其所对应的强大的表达能力