# 已迁移 / MIGRATED

这些 `*.playbook.md` 的**场景 + Good/OK/Bad rubric** 已于 2026-06-07 收编进 storybook 的能力规格
单一来源：`packages/@ooc/meta/storybook/specs/capability_<dim>.md`。

`orchestrate.ts` 现在把 `{PLAYBOOK_PATH}` 指向 storybook specs（不再读本目录）。本目录留作历史参考；
新增/修改场景请改 storybook specs。
