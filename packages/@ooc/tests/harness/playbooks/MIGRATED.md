# 已迁移 / MIGRATED

这些 `*.playbook.md` 的**场景 + Good/OK/Bad rubric** 已收编进 `.ooc-world-meta` 对象树——
各维度对象的 `children/<dim>/knowledge/tests.md`（对象自管自己那片的测试规格）。

`orchestrate.ts` 现在把 `{PLAYBOOK_PATH}` 指向对象树 `children/<dim>/knowledge/tests.md`（不再读本目录）。
本目录留作历史参考；新增/修改场景请改对应维度对象的 `knowledge/tests.md`。
