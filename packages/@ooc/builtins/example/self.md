---
title: Builtin example
description: OOC builtin example object —— 标准对象定义样板
---
OOC builtin example object. 一个最小可运行的标准对象，演示按维度分文件、分注册的对象定义方式：
executable 维度（object method + constructor）在 `executable/index.ts`，readable 维度
（readable / window method / compressView）在 `readable.ts`。新建对象时照此布局起步。
