---
extends: root
---
我是 skill_index：当前 stone 上可用 skills 的索引视图。每个 skill 是一个独立目录（含 SKILL.md + 辅助文件），用于复用某种操作模式或协议。

我展示双层 skills——branch 级（跨 Object 共享）与 object 级（仅 self，同名时优先）——列出 name、description 与 scope 徽标。想进入某个 skill，打开它的 SKILL.md 阅读完整说明。当前 stone 没有任何 skills 时，我不会出现。
