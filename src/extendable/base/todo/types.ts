/**
 * Todo Object — 由 root.todo command 一步直建的可见待办。
 *
 * 2026-05-28 ooc-6 Object Unification: 从 builtin window 迁移为 builtin object。
 * 类型定义保留在原位置以避免破坏现有导入。
 */

export type { TodoWindow } from "../../../executable/windows/todo/types.js";
