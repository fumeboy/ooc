/**
 * Router 模块入口
 *
 * 导出 ViewRegistry 和注册函数。
 */
export { viewRegistry, type ViewProps, type ViewRegistration, type MatchResult } from "./registry";
export { registerAllViews } from "./registrations";
