# internal/utils 指南

## 目的
- 存放与业务解耦的基础工具（配置、日志、校验）。
- 禁止依赖 `agent` / `module`，只依赖标准库。

## 当前子目录
- `config/`：集中式配置解析。

## 统一约定
- 工具函数必须有中文注释说明用途与边界。
- 避免过度抽象：prefer 纯函数 + 明确输入输出。

## TODO
- [ ] 讨论是否需要 logging 子模块。
